app:
  cp:
#    for JCSP
    keystore: HDIMAGE

#    for JCP
#    keystore: HDImageStore

    alias: test2012
    pin: ""
  controller:
    encrypt: "/encr"
    decrypt: "/decr"
    sign: "/sign"
    verify: "/verify"
    tls: "/tls"
    cert: "/cert"
server:
  port: 8080
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
